def f():
	n=int(input("enter the first value"))
	m=int(input("enter the second value"))
	if (n and m) ==0:
		return 1
	else:
		k=n*(n-1)
		j=m*(m-1)
		print("the value of first factorial is:",k)
		print("the value of second factorial is:",j)
	if n==1:
		return m
	else:
		p=(m*n)
		print(p)
